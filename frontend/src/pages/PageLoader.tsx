import { Layout } from '../component';
import { PageLoader as PageLoaderComp } from '../component/common'

export const PageLoader = () => {
  return (
    <Layout Component={<PageLoaderComp/>} />
  )
}